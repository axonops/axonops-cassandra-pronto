---
# This playbook unmounts data volumes.

- name: unmount Cassandra data volumes
  gather_facts: false
  hosts:
    - '{{ host_list }}'
  serial: 1

  tasks:
    - name: unmount volumes
      become: true
      shell: |
        cd /opt/cassandra/scripts
        ./unmount_volumes.sh"
